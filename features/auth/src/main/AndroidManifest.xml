<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feature.auth">

    <application>
        <activity android:name="com.feature.auth.AuthActivity">
            <intent-filter>
                <action android:name="com.azexpress.app.feature.auth.open" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>